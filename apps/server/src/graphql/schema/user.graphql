type User {
  name: String!
  email: String!
}

type Query {
  users: [User!]!
  user(id: ID!): User
}

type Book {
  id: ID!
  title: String!
  authorId: ID!
}

type Mutation {
  addBook(title: String, authorId: ID): Book
}
